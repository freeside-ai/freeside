---
run: manual
stage: plan-upgrade
date: 2026-07-13
branch: docs/plan-v7-upgrade
---

# Plan v7 upgrade

Plan v7 replaces the committed v4 verbatim. The repo skips revisions 5
and 6 (never committed here); their change classes arrive folded into
this replacement, visible through §13's held-from-revision-6 list
(per-stage egress profiles, checkpointed backups, the revision-5
twelve sync tests, the §5.14 sync/conversation model "as revision 6",
out-of-process gauntlet with export-helper boundary,
attended_dev/unattended modes, among others). Deciders per item are
named in §13; revision 7 folds in the sixth external review as
adjudicated.

Revision 7's change classes: candidate-automation blocking and
effective PR-job authority attestation; the workspace-handoff
capability set as the first ward gate; provider_web_read
reclassified as a materially wider credential-exfiltration mode, with
daemon-fetched research artifacts as the 1B elaborator default; honest
best-effort secret-scanning language; auth-store mutation leases split
from inference concurrency; three budget clocks (active-compute,
elapsed deadline, waiting thresholds raising consolidated blocked
items); artifact provenance with trusted publish_eligible;
EvidencePublisher deferred to 1B (schema, provenance enforcement, and
client rendering stay 1A); encrypted backup policy with credential
exclusion; four pairing/revocation tests (suite of sixteen);
condition-driven system_health, versioned start-with-changes, honest
notification statuses, protected client caches, and the narrow setup
elevation boundary; 1A formalized into internal exits 1A.0/1A.1/1A.2;
the deliberately boring first repository (not Freeside); the
classifier never sole gatekeeper of the shadow safety override.

- **The runner subsystem is renamed ward** (formerly envelope; decider:
  user, amended in place on this unmerged revision 7, no bump): register
  consistency with signet/gauntlet/daemon, and "envelope" greps poorly
  against message/HTTP-envelope vocabulary; the naming rule is restated
  as the binding-and-summoning register (plan §13 item 15). Codex
  caught the runner-layer mention in daemon/README.md outside the
  instructed surfaces; the sweep widened to component READMEs (scope
  extends to daemon/ for that one line).
- **§5.12 budget-clock prose aligned to its policy example** (Codex
  P2; the erratum is carried by the user's source v7): the prose named
  `review.hard_wall_time` and called stage budgets "unprefixed" while
  the example defines `review.hard_active_time`, `stage_active_time`,
  and `run_active_compute_time`; the prose now uses the example's
  keys. Wording-level consistency fix (plan §9), no revision bump; the
  source copy still carries the mismatch.
- **The decision history is committed and §13 restructured** (user):
  `docs/history/decisions.md` holds the complete log of every
  revision, including 2, 3, 5, and 6, which were superseded before
  commit and existed nowhere else durable; plan §13 now holds only the
  current revision's items. Standing convention: the history file is
  appended in the same PR as any future plan revision, so the two
  cannot drift. The history is an archaeology file: its older entries
  deliberately retain period terminology (envelope in pre-rename
  revisions) and are never edited.
- **Component READMEs reconciled with the first-repo reversal** (Codex
  P2): app/README.md and policy/README.md said the daemon builds
  itself through Freeside, contradicting v7's deliberately boring
  first repository (§11); both now frame self-management as the
  bootstrap test that follows it. api/ and images/ stay deliberately
  stale per the v4-reconcile decision; nothing else on the maintained
  surfaces claims self-hosting (`grep 'builds itself'` is clean).
- **§5.14 restored to self-contained** (user, closing the review
  finding first declined as author-gated): the prior text delegated
  normative content ("as revision 6", "the twelve of revision 5") to
  revisions superseded before commit and durable nowhere; the
  sync/conversation schemas, discuss transaction, and all sixteen test
  definitions are now inlined.
- **§15 defines coordination vocabulary** (user): lanes take subsystem
  names where one exists, saddle (client lane) and spine (integration
  role, never a territory) are named outside the subsystem register,
  canonical definitions in AGENTS.md's lane glossary; this makes the
  glossary's cross-reference true.
- **Decision-log citations follow the current-only §13** (Codex P2):
  AGENTS.md's provisional-api authority now cites plan §11 Wave 0 plus
  the history, and docs/decisions/README.md seeds ADRs from §13 for
  the current revision and docs/history/decisions.md for the complete
  log; both previously cited decisions that the restructure moved out
  of §13.
- **History preamble matched to the current-only §13** (Codex P2;
  deliberate deviation from the do-not-edit instruction, flagged for
  the user): the preamble claimed §13 carries "an abbreviated
  held-from list", which the restructure removed. The do-not-edit
  rationale protects the archaeology entries' period terminology; the
  preamble is the living contract description, so the false clause was
  dropped. Revision entries untouched.
- **README unchanged** (checked, not overlooked): the root README's
  one-liner and category line match v7 §1 verbatim; no drift, no edit.
- **License ADR-candidate re-deferred** (source:
  `2026-07-08-1051-scaffold-phase0.md`; its `-> re-deferred` clock ran
  out when the v4-reconcile PR merged): open-sourcing remains a
  Phase 4 packaging decision under v7 §11; nothing in this scope
  touches licensing. Marker updated on the source item.

## Verification

- `docs/plan.md` confirmed byte-identical to the source v7 file
  (`diff -q`); the ward rename then amended the file in a follow-up
  commit: eleven exact replacements plus decisions item 15, and the
  §5.12 key erratum was corrected on review. `grep envelope` across
  the repo afterwards matches only item 15's historical mention, this
  entry's own rename records, and docs/history/'s deliberately
  retained period terminology.
- Repo-wide § cross-reference sweep against v7's header list
  (excluding `api/`, `images/`, and frozen devlog entries, per the
  v4-reconcile boundary decision): 26 distinct references, zero
  dangling. v7 renames §5.4/§5.6/§5.7/§5.10 and adds §5.15 plus two
  §11 subsections; numbering of every referenced section is stable.
- AGENTS.md untouched by this PR; managed blocks trivially intact.
