# Wave 4 (1B.0) Planning: The Review Stage, Rescope, and Sweep

Spine wave-planning session (2026-08-02), executing plan revision 26
(PR #441; §11 coordination table row "4 (1B.0): the review stage";
rationale in `devlog/2026-08-01-1643-1b-wave-plan.md`). The wave's unit
list lives on the pinned tracking issue #482; this note records the
precondition verification, the #406/#407 rescope the revision assigned
to this wave, the #405 decision, and the sweep dispositions that would
otherwise exist only in session.

## Precondition Verification

- **Wave 3 units**: #442, #443, #401, #404, #444 verified closed by
  merged PRs (#450, #459, #449, #457, #447); no stragglers.
- **Adversarial review**: the fresh-context audit summary is on #445
  (2026-08-02, audited tip `74aa784`); all six confirmed findings
  #461–#466 verified closed by merged remediation PRs (#468, #473,
  #474, #475, #478); the audit's rejected-by-verification and
  accepted-by-decision entries stand as recorded. Nothing unfiled or
  undispositioned.
- **#427 scheduling gate (§7)**: #401 gates 1/2/4/5 landed in wave 3
  with results permitting a production Codex review pass (gate 1 met:
  daemon-side proactive-refresh model, access-token-only snapshot,
  loud fail-closed expiry; gate 2 closed by topology-and-policy, its
  ward obligations carried into #480; gate 4 decided: append authority
  via a delivered digest-bound file; gate 5 met: explicit minimal
  launcher environment). Gate 3 stays deferred to wave 7 and is not
  applicable: a fresh-context review pass never resumes. Evidence:
  `devlog/2026-08-01-1850-codex-pre-adoption-gates.md`.

Wave 3's exit was recorded and #445 closed at this session; #482 took
the pin.

## The #406/#407 Rescope (Revision 26)

Revision 26 found #406 as written to be the execution contract with no
separable ReviewSource surface, and assigned the split to wave-4
scheduling. Executed as: **new issues carry the review cores; the
original issues keep their numbers as the execution remainders**, so
plan §11 row 7's "the #406/#407 execution remainders" stays literally
accurate and each original issue's dependency history stays attached to
the work it still describes.

- **#479** (review-selection contract core, `kind:contract`): `codex`
  in `domain.AgentVendor`; the #401 gate-4 delivery binding at the
  contract level — append authority via a daemon-composed, digest-bound
  file, superseding #406's replace-authority premise; unconformed
  combinations fail admission typed and closed. Chain position: after
  #443, ahead of #480 and #427. Excluded: StageDriver registration and
  `driver: codex` execution admission (remain #406).
- **#480** (review ward-topology slice, `kind:feature`): #407
  restricted to the §7 review invocation's workspace class (read-only,
  fresh-context, no publication credentials, never resumes), enforcing
  the #401 obligations: `.agents` shadow overlay at root and every
  in-container ancestor plus the fixed severance argv on every start
  (`project_doc_max_bytes=0`, `--ignore-user-config`, `--ignore-rules`;
  the argv, not the shadow, is what severs candidate-controlled
  `AGENTS.md` loading), explicit minimal launcher environment,
  access-token-only snapshot with an admission lifetime floor,
  read-only instruction mount inside the writable `CODEX_HOME`, exact
  `provider_only` egress with the refresh host absent. Excluded:
  writable-workspace/export topology, resume/continuity mount (gate 3),
  and the `.agents/**` editable-round-trip-or-refusal decision (remain
  #407).
- Both remainders re-deferred to the wave-7 execution tail with dated
  comments; #406's remainder is a dormant contract deferral with no
  chain position.

Wave-4 units are `lane:spine` by owner direction: the wave is the
serial, spine-owned review-stage wave (§11 row 4), and #480, though it
lands in ward's package, runs inside that serialized track rather than
as parallel ward-lane work.

## The #405 Decision

**Not scheduled: the review stage does not need a project-derived Codex
image.** The §7 review pass reads the candidate workspace mounted
read-only from the pinned `agent-codex` base (#404) and receives
verification evidence rather than re-running verification; reverify
inside the remediation loop runs the standard §5.15 verification path
with the existing proven project image, independent of the reviewer.
#405 stays deferred to the wave-7 execution tail, carrying its
2026-08-02 builder/toolchain finding (the reusable builder warms npm
from the base image, and the Codex base has no Node): the
provider-independent-toolchain rescope is deliberately left to that
enumeration rather than decided now.

## The Review Anchor (§7 Open Fork)

Deliberately carried unresolved (owner decision; recorded lean:
pre-publication with forge checks still gating merge). Per §7, the
wave-4 chain keeps the PR-anchored shape and cites §7; the fork blocks
nothing and is surfaced on #482, not filed as a blocker.

## Maintainer Prerequisite

**#481** (`needs-human`, no lane, unmilestoned): an enrolled Codex
identity for the production review pass. The gate-1 probes revoked the
host identity's refresh family (surviving access token expires
2026-08-11), so `codex login` re-enrollment, store placement, and
cost-owner confirmation must precede #427's first production pass.
Implementation PRs land on fixtures; the wave's close condition
(real-backlog use begins) blocks on it.

## Deferral Sweep Dispositions

Queue = open + `deferral` + no milestone: 53 items (wave 3's 45, minus
#404 scheduled out, plus #252 labeled in at wave 3 and the eight
post-wave-3 escalations #448, #451, #453, #454, #456, #460, #470,
#472), each examined, each carrying a dated per-issue comment; the
grouped listing is on #482. Judgment calls worth recording:

- **Scheduled into wave 4**: #427 only; the rescope created #479/#480
  as new scheduled units rather than scheduling #406/#407 as written.
- **#448/#451 kept out of the wave**: the review pass runs on the
  manually enrolled identity (#481), and #480 carries the snapshot
  shape and admission lifetime floor, so the review path's ward-topology
  sequencing is satisfied without the daemon-owned refresh/enrollment
  lifecycle. Sustained unattended review does need both, so they carry
  wave-5 affinity (auth-identity territory, with
  `max_parallel_executions`) rather than the wave-7 drain.
- **#381 re-examined as wave 3 promised**: the review pass consumes
  verification evidence, never agent claims (§5.15/§7), so the durable
  claim surface is not review-stage substrate; moved to wave-5 affinity
  (EvidencePublisher makes the claim/evidence surfaces real).
- **Wave-5 affinity, unchanged**: #316, #241, #360; joined by #470
  (real-backlog ergonomics begin at wave-4 close) and #472
  (serializes with the EvidencePublisher unit).
- **Wave-7 / 1B.1 affinity**: #453/#454 (supervision, per #455's
  recorded deferral), #456 (consumes #466's corrected behavior), #460.
- **Wave-7 drain**: the 28 wave-3 drain items plus #252, unchanged.
- **Dormant contract deferrals, no chain position**: #361, #360, #265,
  #241, #22, #397, #453, and #406's remainder.
- **Standing conditions unchanged**: #30, #15, #13.

## Spine Repair: #458

#458 was open with neither a `deferral` label nor a milestone — outside
the sweep queue by definition, invisible to every future drain.
Labeled `deferral` + `lane:saddle` with a dated comment. Noted for the
wave-7 drain: `scripts/test-install-mac-app.sh` now exists on `main`
(landed with the #464 remediation), so the remaining scope is the delta
against the issue's declared matrix, possibly empty.

## Revisit When

- The §7 review-anchor fork resolves: a pre-publication anchor reshapes
  #427's chain placement and the wave 4/6 boundary (revision 26's
  standing condition).
- Wave-5 scheduling begins: re-examine #448, #451, #316, #241, #360,
  #381, #470, #472.
- A Codex identity is re-enrolled (#481): isolate gate 1's accepted
  residual (a refresh succeeding against a read-only store) cheaply,
  before any resume-capable execution work.
