# Wave 3 (1B.0) Planning: Decomposition and Deferral Sweep

Spine wave-planning session (2026-08-01), executing plan revision 26
(PR #441; §11 coordination table row "3 (1B.0): loop foundations";
rationale in `devlog/2026-08-01-1643-1b-wave-plan.md`). The wave's unit
list lives on the pinned tracking issue #445; this note records the
precondition verification, the decomposition's owner-relevant choices,
and the sweep dispositions that would otherwise exist only in session.

## Precondition Verification

- **Wave 2 units**: every issue on #231's current-authority chain
  (prerequisites, critical chain, opportunistic follow-ups, maintainer
  prerequisites, safety gates) verified closed; no stragglers.
- **Adversarial review**: the fresh-context audit summary is on #231
  (2026-07-31, audited SHA `2a1a90d`); confirmed findings #411–#413 all
  closed, fifteen rejected-by-verification with evidence, nine
  accepted-by-decision as owner posture. Nothing unfiled or undispositioned.
- **Verified 1A exit**: recorded as the "Wave 2 / Phase 1A exit"
  evaluation on #231 (2026-08-01 comment), which dispositions each §11
  exit condition; the one recorded gap (§10 clean-machine packaging
  checks) is owner-deferred to #428. The 1A milestone's closure was not
  treated as the verification; the exit comment is the citable record.

## Decomposition

Three parallel lanes, milestone 1B, targeting the 1B.0 exit; no
declared-path overlap between lanes.

- **Spine, serialized contract track**: #442 (§5.16 scheduler: closed
  kind union, the four consumer-backed timer kinds, permanent
  trusted-config jobs, doctor/janitor/onboarding-poll ticker migration
  preserving §10 obligations in `attended_dev`), then #443 (§5.18
  capture-hook recording; recording only, no projection). Acceptance on
  both is the plan section text verbatim, so the spec cannot drift from
  the issue. Both are inserted into the serialized contract chain ahead
  of the wave-4 review-selection core and #427, per revision 26's chain
  order; #427's position is recorded on its thread without scheduling it.
- **Ward**: existing #401 and #404 scheduled, not recreated. #401 is
  rescoped at scheduling: gates 1/2/4/5 are wave-3 acceptance as
  parallel probes; gate 3 (poisoned-rollout resume) is recorded on the
  issue as deferred to the wave-7 execution tail, because a
  fresh-context review pass never resumes. #404 stays blocked on gate
  2's outcome (workspace-skill severance can dictate the pinned CLI
  version).
- **Saddle**: #444, Mac-first operator access per §10's install
  paragraph (install/update script, §15 identity icon, personal-team
  signing for Keychain stability across rebuilds, real device pairing).
  iOS explicitly out of scope (mid-1B, wave 6).
- **No needs-human filings**: no unit this wave depends on a
  maintainer-only action; the paid Apple Developer Program is Phase 2
  by owner decision and was deliberately not filed.

## Deferral Sweep Dispositions

Queue = open + `deferral` + no milestone: 45 items, each examined, each
carrying a dated per-issue comment; the grouped listing is on #445.
Judgment calls worth recording:

- **Scheduled into wave 3**: #404 only (plus #401, which was open and
  unmilestoned but never deferral-labeled). Revision 26 concentrates
  the hardening drain in wave 7, so the 28 hardening/cleanup items were
  re-deferred to that wave's sweep-eligibility enumeration rather than
  thinly spread across waves 3–6.
- **Wave-4 affinity, re-examined at that wave's scheduling**: #406 and
  #407 (split into review cores and execution remainders at wave-4
  scheduling; no chain position assigned to either as written), #405
  (conditional on review needing a project-derived image), #427 (chain
  position assigned, deferral retained), #381 (durable review surface).
- **Wave-5 affinity**: #316, #241, #360 — all keyed to the multi-writer
  posture or state-algebra territory that wave 5's
  `max_parallel_executions` experiment and contract drain make real.
- **Dormant contract deferrals excluded without chain positions**:
  #361, #360, #265, #241, #22, #397 (#406 receives its position at the
  wave-4 rescope). Only #427 received a position, since revision 26
  already fixed its order.
- **Standing conditions unchanged**: #30, #15, #13.

## Spine Repair: #252

#252 carried milestone 1B while listed on no tracking issue; either
scheduling field alone is a spine-repair error (AGENTS.md, Work units).
It is not a wave 3–6 unit under revision 26's decomposition, so it was
unmilestoned rather than listed: milestone removed, `deferral` label
added so it stays sweep-visible, dated comment posted. It re-enters
through the wave-7 drain enumeration or earlier owner fiat. Rejected
alternative: listing it into wave 3 as a fourth lane — rejected because
revision 26's row names three lanes and #252 (publish-lane work-account
opt-in) serves no 1B.0 exit condition.

## Revisit When

- Wave-4 scheduling begins: the #406/#407 rescope must land before the
  review-selection core is claimable, and the §7 review-anchor fork
  needs its owner decision confirmed (revision 26's Revisit-when).
- Wave-5 scheduling begins: re-examine #316, #241, #360.
- The wave-7 drain enumeration: #252 and the 28 re-deferred hardening
  items return to the table.
