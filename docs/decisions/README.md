# Decisions (ADRs)

This directory holds Architecture Decision Records promoted from devlog entries per the devlog protocol in `AGENTS.md`: one file per decision, named `NNNN-slug.md`.

There are no ADRs yet. The [key decisions log in `docs/plan.md` §9](../plan.md) is the seed; each decision there will be split into its own ADR when it is first revisited, with the originating devlog entry as its citation.
