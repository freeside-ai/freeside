// Package domain holds the daemon's shared domain types and their
// validation: the vocabulary every lane depends on. It is spine-owned
// and changes only through kind:contract work units.
//
// See docs/plan.md §4 (the attention model) and §5 (architecture); the
// concrete types land with Wave 0 unit 2 (the domain package).
//
// Placeholder for Wave 0 unit 1 (repo and toolchain bootstrap).
package domain
