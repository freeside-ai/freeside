// Package publish owns GitHub App authentication, deterministic
// identities, reconciliation plus intake, and the EvidencePublisher.
//
// Lane: publish. See docs/plan.md §5.5 (the CI trust boundary), §5.9
// (durability: effectively-once), §5.11 (GitHub integration:
// reconciliation plus intake), and §5.15 (evidence and images).
//
// Placeholder for Wave 0 unit 1 (repo and toolchain bootstrap); real
// types and logic land with later units.
package publish
